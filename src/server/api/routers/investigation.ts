// Investigation-Router deaktiviert, da das Model nicht mehr existiert.
import { createTRPCRouter } from "~/server/api/trpc";

export const investigationRouter = createTRPCRouter({
  // Alle Investigation-Routen sind aktuell deaktiviert, da das Model im Prisma-Schema fehlt.
}); 